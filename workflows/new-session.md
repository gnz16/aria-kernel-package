---
description: Reset ARIA APM state for a new project while archiving history.
---

1. Run the session reset script
   running: python3 _scripts/reset_session.py
   // turbo
2. Verify the reset
   running: cat _scripts/appm_state.json
